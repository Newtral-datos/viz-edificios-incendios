{
  "name": "viz-edificios-incendios",
  "private": true,
  "version": "0.0.0",
  "type": "module",
  "scripts": {
    "postinstall": "node scripts/copy-maplibre-worker.mjs",
    "prepare-data": "python3 scripts/prepare_data.py",
    "dev": "vite",
    "build": "vite build",
    "preview": "vite preview",
    "predeploy": "npm run build",
    "deploy": "gh-pages -d dist"
  },
  "devDependencies": {
    "@sveltejs/vite-plugin-svelte": "^7.1.2",
    "gh-pages": "^6.3.0",
    "playwright": "^1.61.1",
    "svelte": "^5.56.4",
    "vite": "^8.1.1"
  },
  "dependencies": {
    "maplibre-gl": "^6.0.0",
    "pmtiles": "^4.4.1"
  }
}
